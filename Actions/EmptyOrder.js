import * as actionTypes from "../Actions/actionTypes";
 
const DeleteItem = (  ) => {
    return ({
        type:actionTypes.EMPTY_ITEMS,
        item:[]
    })
   }
   
   export default  DeleteItem 